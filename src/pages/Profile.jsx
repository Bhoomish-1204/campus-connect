import React,{useState} from 'react'
import {Save,UserCircle} from 'lucide-react'
import {supabase} from '../lib/supabase'
export default function Profile({profile,refresh}){const [form,setForm]=useState({full_name:profile?.full_name||'',department:profile?.department||'',year:profile?.year||'',bio:profile?.bio||'',skills:profile?.skills||''});const [msg,setMsg]=useState('');async function save(e){e.preventDefault();const {data:u}=await supabase.auth.getUser();const {error}=await supabase.from('profiles').update(form).eq('id',u.user.id);setMsg(error?error.message:'Profile updated successfully.');if(!error)refresh()}return <div><div className="page-head"><div><span className="eyebrow">YOUR PROFILE</span><h1>Profile</h1><p>Complete your profile to unlock more relevant recommendations.</p></div></div><div className="profile-layout"><div className="profile-card"><div className="big-avatar">{(form.full_name||'S')[0].toUpperCase()}</div><h2>{form.full_name||'Student'}</h2><p>{profile?.email}</p><span className="pill">{profile?.role||'student'}</span></div><form className="form-card" onSubmit={save}><div className="form-grid"><label>Full name<input value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})}/></label><label>Department<input value={form.department} onChange={e=>setForm({...form,department:e.target.value})} placeholder="Computer Science"/></label><label>Year<input value={form.year} onChange={e=>setForm({...form,year:e.target.value})} placeholder="3rd year"/></label><label>Skills<input value={form.skills} onChange={e=>setForm({...form,skills:e.target.value})} placeholder="Python, React, SQL"/></label></div><label>About you<textarea rows="5" value={form.bio} onChange={e=>setForm({...form,bio:e.target.value})} placeholder="Tell the campus community about your interests..."/></label>{msg&&<div className="success">{msg}</div>}<button className="primary" type="submit"><Save size={17}/> Save changes</button></form></div></div>}
