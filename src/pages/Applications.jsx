import React,{useEffect,useState} from 'react'
import {ClipboardList,ExternalLink} from 'lucide-react'
import {supabase} from '../lib/supabase'
export default function Applications(){const [items,setItems]=useState([]);useEffect(()=>{load()},[]);async function load(){const {data}=await supabase.from('applications').select('*,opportunities(*)').order('created_at',{ascending:false});setItems(data||[])}return <div><div className="page-head"><div><span className="eyebrow">TRACK PROGRESS</span><h1>Applications</h1><p>Keep every opportunity and its current status in one place.</p></div></div><div className="application-list">{items.map(a=><article className="application" key={a.id}><div className="app-icon"><ClipboardList size={21}/></div><div className="app-main"><h3>{a.opportunities?.title||'Opportunity'}</h3><p>{a.opportunities?.organization||'Organization'} · Applied {new Date(a.created_at).toLocaleDateString()}</p></div><span className={`status ${a.status}`}>{a.status.replace('_',' ')}</span>{a.opportunities?.apply_url&&<a href={a.opportunities.apply_url} target="_blank" rel="noreferrer"><ExternalLink size={17}/></a>}</article>)}{!items.length&&<div className="empty"><ClipboardList size={25}/><h3>No applications yet</h3><p>When you apply to opportunities, you can track them here.</p></div>}</div></div>}
