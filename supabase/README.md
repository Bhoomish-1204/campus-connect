# Supabase setup

Run `schema.sql` in the Supabase SQL Editor. It creates tables, authentication profile trigger, RLS policies and sample records.

For Google login, enable Google under Authentication -> Providers and configure the OAuth credentials there.
